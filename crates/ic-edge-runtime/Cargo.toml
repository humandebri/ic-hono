[package]
name = "ic-edge-runtime"
version = "0.2.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
rust-version.workspace = true
description = "QuickJS-based Worker-compatible runtime for running Hono bundles on the Internet Computer."
keywords.workspace = true
categories.workspace = true
readme.workspace = true

[features]
default = []
quickjs-ic = ["dep:quickjs-wasm-rs"]

[dependencies]
ic-edge-web = { version = "0.2.0", path = "../ic-edge-web" }
hmac = "0.12"
sha2 = "0.10"

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
getrandom = "0.2"
rquickjs = "0.11.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[target.'cfg(target_arch = "wasm32")'.dependencies.quickjs-wasm-rs]
version = "3.1.0"
optional = true

[target.'cfg(target_arch = "wasm32")'.dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
path = "src/lib.rs"
